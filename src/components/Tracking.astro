---
const areWeLive: boolean = Astro.url.hostname !== 'localhost';
---
{areWeLive && <>
	<!-- Microsoft Clarity - Only loads with user consent -->
	<script>
		// Check for cookie consent before loading tracking scripts
		const CONSENT_KEY = 'cookie-consent';
		const consent = localStorage.getItem(CONSENT_KEY);
		const areWeLive = window.location.hostname !== 'localhost';

		if (consent === 'accepted' && areWeLive) {
			// Load Microsoft Clarity
			(function(c,l,a,r,i,t,y){
				c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
				t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
				y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
			})(window, document, "clarity", "script", "u98zkgu0rt");
		}

		// Listen for consent changes
		window.addEventListener('cookieConsentChange', function(e) {
			if (e.detail.consent === 'accepted') {
				// User just accepted - tracking will load on page reload
				console.log('Analytics consent granted');
			}
		});
	</script>
<>}
